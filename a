// 로컬 -> 원격저장소 업로드는
git push -u 원격저장소주소 올릴로컬브랜치명 

git remote add 변수명 주소
git remote add origin 깃주소

주소가 -> 변수명으로 들어감

git push -u 추가하면 주소를 기억하라는뜻
앞으로는 git push 만 해도됨

git push 밀어넣기
git pull 당겨오기

push 하기 전에 pull 먼저해서 원격저장소에서 소스 가져와야함

git clone 주소 // 깃허브에서 소스 가져오기
clone에서 가져온 소스 push 하려면 원격저장소에 권한설정해줘야함

SVN commit 하기전에 syn 맞추는것과 같음